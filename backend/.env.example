PORT=5000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=attendance
DB_USER=postgres
DB_PASSWORD=h1o2p3e4@2010

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this
JWT_REFRESH_SECRET=your_super_secret_refresh_key_change_this
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Nodemailer (Gmail — use an App Password, not your real password)
EMAIL_USER=staffnetrca@gmail.com
EMAIL_PASS=mtiz bsmm vppv cvel

# Patron (discipline teacher) email
PATRON_EMAIL=krbenon@gmal.com

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000
